pipeline {
    agent any

    stages {
        stage('Build') {
            steps {                
                sh "cp -r index.html /Users/simran/Downloads/Code/test"
                echo "Building..."
            }
        }
    }
}
