pipeline {
    agent any

    stages {
        stage('Build') {
            steps {                
                sh "cp -r index.hrml /Users/simran/Downloads/Code/test"
                echo "Building..."
            }
        }
    }
}
